可以按“先搭底层、再搭状态机、最后接记录回放”的顺序做，比较稳。

**一、先确定总体架构**

建议用一个 `1ms SysTick` 做系统节拍，不要在主循环里长时间阻塞。

核心模块可以拆成：

```text
main
 ├─ key_app        按键扫描/短按/组合长按
 ├─ lcd_app        4个界面刷新
 ├─ adc_app        R37电压采集
 ├─ temp_app       DS18B20温度采集
 ├─ pwm_in_app     PA1频率/占空比测量
 ├─ pwm_out_app    PA7回放输出
 ├─ record_app     记录与回放状态机
 ├─ alarm_app      报警判断与计数
 └─ led_app        LD1~LD8状态输出
```

系统状态建议分三层：

```c
typedef enum {
    PAGE_DATA = 0,
    PAGE_PARA,
    PAGE_RECD,
    PAGE_FSET
} Page_t;

typedef enum {
    SYS_IDLE = 0,
    SYS_RECORDING,
    SYS_PLAYBACK_PULSE,
    SYS_PLAYBACK_VOLT
} RunMode_t;

typedef struct {
    uint16_t FH;     // 1000~10000, default 2000
    float AH;        // 0.0~3.3, default 3.0
    uint8_t TH;      // 0~80, default 30
    uint8_t FP;      // 1~10, default 1
    float VP;        // 0.0~3.3, default 0.9
    uint8_t TT;      // 2~10, default 6
} Param_t;
```

---

**二、底层外设实现顺序**

1. **LCD**
   - 先完成黑底白字。
   - 固定4个界面：`DATA / PARA / RECD / FSET`。
   - 每100ms刷新一次即可，不必每轮主循环狂刷。

2. **LED**
   - 做一个统一接口，例如：
     ```c
     LED_Set(uint8_t led, uint8_t state);
     LED_AllOff(void);
     ```
   - LD1/LD2/LD3用100ms节拍翻转。
   - LD4/LD5/LD6由报警状态控制。
   - LD7/LD8始终关闭。

3. **按键**
   - 10ms扫描一次。
   - 软件防抖。
   - 产生短按事件：`B1_SHORT / B2_SHORT / B3_SHORT / B4_SHORT`。
   - 单独检测 `B3+B4` 同时按下超过2秒，触发恢复初始状态。
   - 记录期间所有按键事件丢弃。

4. **ADC电压**
   - 周期采样R37。
   - 换算：
     ```c
     voltage = adc_value * 3.3f / 4095.0f;
     ```
   - 显示保留1位小数。
   - 报警判断用原始浮点值或扩大10倍的整数值均可。

5. **DS18B20**
   - 建议做成非阻塞流程：
     - 发起温度转换。
     - 等待约750ms。
     - 读取温度。
   - 温度刷新周期控制在1s以内。
   - 显示保留1位小数，报警阈值TH是整数。

6. **PA1输入捕获**
   - 用定时器输入捕获或PWM Input模式测频率与高电平时间。
   - 计算：
     ```c
     F = timer_clk / period_count;
     D = high_count * 100 / period_count;
     ```
   - 加入超时处理：一段时间没有捕获到边沿时，频率和占空比清零。
   - 注意全量程频率精度要求±3%，定时器时钟和预分频要选好。

7. **PA7 PWM输出**
   - 封装两个接口：
     ```c
     PWM_Set(uint32_t freq, float duty);
     PWM_StopLow(void);
     ```
   - 回放结束后必须停止PWM，并让PA7输出低电平。

---

**三、界面与按键逻辑**

**DATA界面：**
- B1：切到PARA。
- B2：开始记录。
- B3：如果已有记录，开始电压回放。
- B4：如果已有记录，开始脉冲回放。

**PARA界面：**
- B2：选择 `FH -> AH -> TH`。
- B3：当前参数加。
- B4：当前参数减。
- 参数边界：
  - FH：1000~10000，每次1000。
  - AH：0.0~3.3，每次0.3。
  - TH：0~80，每次1。

**RECD界面：**
- B2：清零 `FN / AN / TN`。
- B1：切到FSET。

**FSET界面：**
- B2：选择 `FP -> VP -> TT`。
- B3：当前参数加。
- B4：当前参数减。
- 参数边界：
  - FP：1~10，每次1。
  - VP：0.0~3.3，每次0.3。
  - TT：2~10，每次2。

**B3+B4长按：**
- 任意界面恢复初始状态：
  - 当前界面回到DATA。
  - 参数恢复默认值。
  - 报警次数清零。
  - 停止记录/回放。
  - PA7输出低电平。

---

**四、报警逻辑**

报警次数要求是“实时值 > 上限时累加一次，持续报警不重复累加”。

所以每一项都需要一个上次报警状态：

```c
if (freq > FH) {
    if (!freq_alarm_last) FN++;
    freq_alarm_now = 1;
} else {
    freq_alarm_now = 0;
}
freq_alarm_last = freq_alarm_now;
```

电压、温度同理。

LD状态：

```text
LD4 = F > FH
LD5 = A > AH
LD6 = T > TH
```

---

**五、记录功能**

建议记录周期用100ms。

最大TT=10秒，所以最多：

```text
10s / 0.1s = 100组数据
```

数据结构：

```c
#define RECORD_MAX 100

typedef struct {
    uint16_t freq;
    uint8_t duty;
    float voltage;
} RecordItem_t;

RecordItem_t record_buf[RECORD_MAX];
uint8_t record_len;
uint8_t record_index;
uint8_t has_record;
```

记录流程：

1. DATA界面按B2。
2. `run_mode = SYS_RECORDING`。
3. 清空旧记录。
4. 每100ms记录一次当前：
   - R37电压。
   - PA1频率。
   - PA1占空比。
5. LD1每100ms闪烁。
6. 记录满 `TT * 10` 个点后结束。
7. `has_record = 1`。
8. LD1熄灭，回到空闲状态。

记录期间按键全部无效。

---

**六、脉冲回放**

DATA界面下，且已有记录，按B4触发。

流程：

1. `run_mode = SYS_PLAYBACK_PULSE`。
2. 每100ms取一条记录。
3. 输出频率：
   ```c
   out_freq = record_freq / FP;
   ```
4. 占空比保持记录值：
   ```c
   out_duty = record_duty;
   ```
5. 如果频率为0，PA7输出低电平。
6. LD2每100ms闪烁。
7. 回放 `TT` 秒后停止，PA7输出低电平，LD2熄灭。

---

**七、电压回放**

DATA界面下，且已有记录，按B3触发。

流程：

1. `run_mode = SYS_PLAYBACK_VOLT`。
2. PA7固定输出1kHz。
3. 每100ms取一条记录电压值。
4. 根据试题图6，把电压转换成占空比。
5. 使用VP作为电压回放最小值参数。
6. LD3每100ms闪烁。
7. 回放结束后PA7输出低电平，LD3熄灭。

建议把图6关系单独封装：

```c
uint8_t VoltageToDuty(float voltage, float VP)
{
    // 按试题图6实现映射关系
}
```

这样后面如果图6公式确认了，只需要改这一个函数。

---

**八、主循环调度建议**

主循环只做轻量任务：

```c
while (1)
{
    Key_Task();          // 10ms
    ADC_Task();          // 50~100ms
    Temp_Task();         // <=1s
    PWM_Input_Task();    // 捕获结果整理/超时判断
    Alarm_Task();        // 100ms
    Record_Task();       // 100ms
    Playback_Task();     // 100ms
    LED_Task();          // 100ms
    LCD_Task();          // 100~200ms
}
```

关键是不要让DS18B20、LCD刷新、延时函数阻塞按键和采集。

---

**九、推荐实现顺序**

1. 点亮LCD，完成4个静态界面。
2. 完成B1切换界面。
3. 完成参数默认值、边界调整、选中项切换。
4. 完成ADC显示电压。
5. 完成DS18B20显示温度。
6. 完成PA1频率和占空比测量。
7. 完成报警判断、报警次数、LD4~LD6。
8. 完成B2记录，验证LD1闪烁和记录数组。
9. 完成PA7基础PWM输出。
10. 完成脉冲回放。
11. 完成电压回放。
12. 完成B3+B4长按恢复。
13. 最后统一检查响应时间、边界值、显示格式和提交命名。

这个题最容易丢分的地方是：报警次数重复累加、记录期间按键仍然生效、PA7回放结束没有拉低、参数越界、DS18B20阻塞导致按键不灵。把这几个点提前按状态机处理，整体会顺很多。